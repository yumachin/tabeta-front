"use client";

export default function FollowedPhotoButton() {
  const handleClick = () => {
    // const posts = await api
  };

  return (
    <button 
      className="flex-1 py-3 font-medium text-center text-gray-500"
      onClick={handleClick}
    >
      フォロー中
    </button>
  )
};